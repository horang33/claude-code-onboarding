# Block 0: Setup (~10분)

> 퀴즈 없음. Phase A만 진행 후 바로 Block 1로.

---

## EXPLAIN

### Claude Code란?

Claude Code는 터미널(명령줄)에서 동작하는 AI 코딩 에이전트입니다.
"코딩"이라는 이름이 붙었지만, 실제로는 **파일 읽기/쓰기, 웹 검색, 외부 도구 연결** 등
다양한 작업을 자동화할 수 있는 범용 AI 어시스턴트입니다.

### 디자이너/리서처/PM에게 왜 필요한가?

- **Figma** 파일을 읽고 디자인 시스템을 분석할 수 있습니다
- **Notion** 페이지를 검색하고 내용을 정리할 수 있습니다
- **Slack** 대화를 검색하고 요약할 수 있습니다
- 인터뷰 녹취록을 구조화된 분석 리포트로 변환할 수 있습니다
- 반복 업무를 "레시피"로 만들어 한 번에 실행할 수 있습니다
- 제품 스펙을 자동으로 정리하고 우선순위를 매길 수 있습니다
- 경쟁사 정보를 수집하고 비교 분석할 수 있습니다

이 모든 것이 **하나의 도구**에서 가능합니다.

### 설치 확인

사전 준비 가이드를 완료하셨다면 이미 설치가 되어 있습니다.
아래 명령어로 확인해보세요:

```bash
claude --version
```

버전 번호가 출력되면 준비 완료입니다.

> 아직 설치하지 않았다면 `references/pre-setup-guide.md`의 안내를 따라주세요.
> Node.js나 npm 설치는 필요 없습니다. 네이티브 설치 한 줄이면 됩니다.

---

## EXECUTE

### 실습 1: 첫 대화

터미널에서 `claude`를 실행하고, 아래를 입력해보세요:

```
안녕! 나는 {역할}이야. 오늘 Claude Code를 처음 써봐.
```

> `{역할}` 부분을 본인 역할(디자이너/리서처/PM)로 바꿔 입력하세요.

Claude가 응답하면 성공입니다.

### 실습 2: MCP 연결 상태 확인

> MCP는 Claude Code에서 Figma, Notion 같은 외부 도구를 연결하는 기능입니다. 자세한 내용은 Block 4에서 배웁니다.

Claude Code 대화창에서 아래를 입력해보세요:

```
연결된 MCP 서버 목록을 보여줘
```

Claude가 현재 연결된 MCP 서버를 알려줍니다. 아래 항목이 있는지 확인하세요:

- **Figma** (디자이너에게 필수)
- **Notion** (리서처/PM에게 필수)
- **Slack** (공통)

> 만약 MCP 서버가 없다면, 나중에 Block 4에서 설정하는 방법을 배웁니다.
> 지금은 Claude Code가 정상 실행되는 것만 확인하면 됩니다.

### 실습 3: 작업 폴더 만들기

오늘 실습에 사용할 폴더를 만들겠습니다:

```bash
# Claude Code 대화창에서 입력
내 홈 디렉토리에 claude-practice 폴더를 만들어줘
```

---

> 직접 실행해보세요! 3가지 실습을 마치면 "완료"라고 입력해주세요.
